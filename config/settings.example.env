# Prediction Market Trading System - Configuration Template
# Copy this file to .env and fill in actual values

# Database Configuration
DB_PATH=prediction_market.db
DB_POOL_SIZE=5
DB_TIMEOUT=30

# Core Service Configuration
LOG_LEVEL=INFO
INGEST_INTERVAL_S=5
CONSTRAINT_CHECK_INTERVAL_S=10
PNL_SNAPSHOT_INTERVAL_S=300
MODEL_REFIT_INTERVAL_S=3600

# Execution Service Configuration
MAX_ORDER_RETRIES=3
RETRY_BACKOFF_BASE_S=1
ORDER_TIMEOUT_S=30

# Polymarket Configuration
POLYMARKET_RPC_URL=https://polygon-rpc.com
POLYMARKET_CHAIN_ID=137
POLYMARKET_AMM_CONTRACT=0x0000000000000000000000000000000000000000
POLYMARKET_USDC_CONTRACT=0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174
POLYMARKET_PRIVATE_KEY=
POLYMARKET_WALLET_ADDRESS=

# Kalshi Configuration
KALSHI_API_KEY=
KALSHI_API_SECRET=
KALSHI_API_BASE=https://trading-api.kalshi.com/trade-api/v2

# Market Data Configuration
PRICE_FETCH_INTERVAL_S=10
POLYMARKET_API_BASE=https://api.polymarket.com
POLYMARKET_CLOB_API_BASE=https://clob.polymarket.com

# EU SOCKS5 proxy for Polymarket (optional — leave blank to connect directly)
# Format: socks5://host:port
POLYMARKET_PROXY=
KALSHI_PUBLIC_API_BASE=https://api.kalshi.com

# Constraint Configuration
MAX_POSITION_SIZE=10000
MAX_DAILY_LOSS=50000
MAX_CONCURRENT_POSITIONS=20
MAX_MARKET_EXPOSURE=5000

# Signal Configuration
SIGNAL_EXPIRY_S=300
ORDER_EXECUTION_MODE=simultaneous
ABORT_ON_PARTIAL=false

# Phase 1 cross-platform arbitrage profitability
# Absolute expected-net-profit quality floor, applied after modeled fees,
# slippage, and other pre-trade execution costs. Keep this at $0.50 for the
# initial $100-capital validation phase.
PHASE1_MIN_NET_PROFIT=0.50
# Percentage/net-edge gate remains a separate requirement.
PHASE1_MIN_NET_EDGE=0.005

# Phase 1 paper-validation bankroll and risk limits.
# These produce approximately $5 max/trade, $20 max portfolio exposure,
# and $2 daily loss at the initial $100 bankroll.
STARTING_CAPITAL=100
MAX_POSITION_PCT=0.05
MAX_PORTFOLIO_EXPOSURE_PCT=0.20
MAX_DAILY_LOSS_PCT=0.02

# Phase 1 is the only strategy being validated initially.
STRATEGY_P2_ENABLED=false
STRATEGY_P3_ENABLED=false
STRATEGY_P4_ENABLED=false
STRATEGY_P5_ENABLED=false

# Analytics Configuration
ANALYTICS_OUTPUT_DIR=analytics_export
ENABLE_PNL_TRACKING=true
ENABLE_VIOLATION_LOGGING=true

# Development Configuration
DEBUG=false
ENABLE_MOCK_CLIENTS=false

# Execution Mode: paper uses live public market data/books but never submits exchange orders.
EXECUTION_MODE=paper

# Feature Flags
ENABLE_AUTO_PAIR_DISCOVERY=true
ENABLE_CONSTRAINT_ENFORCEMENT=true
ENABLE_SIGNAL_GENERATION=true
ENABLE_EXECUTION_SERVICE=true

# Mock Execution Configuration (legacy mock client only)
MOCK_FILL_PROBABILITY=0.95
MOCK_PARTIAL_FILL_PROBABILITY=0.05
MOCK_PARTIAL_FILL_FRACTION=0.6
MOCK_MIN_LATENCY_MS=50
MOCK_MAX_LATENCY_MS=300
MOCK_MIN_FILL_LATENCY_MS=200
MOCK_MAX_FILL_LATENCY_MS=2000
MOCK_MAX_SLIPPAGE=0.01
MOCK_FEE_RATE=0.02
MOCK_VERBOSE=true
